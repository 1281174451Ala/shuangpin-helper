# 双拼学习辅助

此上下文描述双拼学习悬浮工具中的输入辅助和用户偏好概念，避免把悬浮交互、设置交互与方案数据混为一谈。

## 窗口与外观

**悬浮窗口**：
显示双拼键盘的非交互式、置顶辅助窗口，不接收用于编辑设置的输入。
_Avoid_：主窗口、设置窗口

**设置窗口**：
由菜单栏或托盘打开、可获得焦点的独立窗口，用于编辑并预览应用偏好。
_Avoid_：系统设置、悬浮设置页

**空闲淡化**：
悬浮窗口在一段无活动时间后降低不透明度，并在活动发生时立即恢复的外观行为。
_Avoid_：窗口虚化、背景模糊

**外观模式**：
决定悬浮窗口与设置窗口使用深色、浅色或跟随 macOS 系统外观的用户偏好。
_Avoid_：任意配色、状态配色

**跟随系统**：
外观模式的一种取值，持续以 macOS 当前深浅色外观解析有效主题。
_Avoid_：启动时读取系统外观

**不淡化**：
空闲淡化的一种取值，悬浮窗口保持完整不透明度，不因无活动时间改变外观。
_Avoid_：零秒淡化

## 双拼方案

**当前方案**：
应用已选定、用于渲染键帽映射和计算合法第二键的内置双拼方案；首版固定为小鹤双拼。
_Avoid_：用户输入法、运行时自定义方案

**方案注册表**：
应用内可供当前方案选择的、已校验方案数据的集合；首版只登记小鹤双拼。
_Avoid_：远程方案市场、用户配置文件

## 偏好持久化

**应用设置**：
由原生层在应用配置目录中持久化、供设置窗口和悬浮窗口共同使用的版本化用户偏好。
_Avoid_：浏览器本地存储、窗口位置文件

**即时设置**：
用户改变偏好后立即写入应用设置并同步到悬浮窗口的交互语义，不存在未保存草稿。
_Avoid_：保存并应用、设置草稿

**设置恢复**：
应用设置无法读取或校验时，以默认偏好继续运行并保留原文件副本的降级规则。
_Avoid_：启动失败、静默重置
