# ADR 013：持久化最小化的 MVP 设置

## 状态

已接受

## 背景

用户需要按自己的屏幕和偏好调整悬浮键盘，但过早提供方案切换、开机启动等设置会扩大原生实现和测试范围。

## 决策

MVP 仅持久化窗口位置、缩放、透明度、是否置顶，以及是否暂停监听；应用下次启动时恢复这些设置。输入方案固定为小鹤双拼，开机启动不在 MVP 范围内。

## 影响

设置界面和持久化模型保持小而实用。未来加入其他双拼方案或开机启动时，需要扩展设置模型并补充迁移与原生平台测试。
