# ADR 012：等待第二键状态不设超时

## 状态

已接受

## 背景

用户输入双拼第一键后，可能停下来查看键位或思考下一步。自动超时会让候选提示在用户仍需要时消失。

## 决策

等待第二键状态不设置自动超时。只有收到第二个字母键、`Backspace` 或 `Escape` 时才改变该状态。

## 影响

慢速学习和正常连续输入均得到稳定提示。用户若在其他场景输入一个字母后不继续，候选状态会持续到下一次受支持的状态变化事件。
