# ADR 017：授权后重新检测并自动恢复监听

## 状态

已接受

## 背景

用户可能在应用运行期间前往系统设置授予辅助功能权限。要求用户重启应用会增加学习工具的使用摩擦。

## 决策

应用在窗口重新获得焦点、打开设置，以及用户点击“重新检测权限”时检查辅助功能权限。检测到权限已授予后，自动恢复全局键盘监听，无需重启应用。首次弹出授权提示后也会立即重新检测并启动监听。

## 影响

权限状态可及时恢复，设置界面需包含重新检测入口。原生层需能安全地重复检查权限并幂等地启动监听，避免重复注册监听器。
