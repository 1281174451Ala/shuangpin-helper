# ADR 010：虚拟键盘仅显示双拼字母键

## 状态

已接受

## 背景

本工具的目标是帮助用户记忆双拼键位和下一步候选，不承担完整键盘的展示或输入职责。显示数字、符号、修饰键等无关按键会分散注意力并占用悬浮窗口空间。

## 决策

虚拟键盘仅渲染 26 个用于双拼的字母键，不显示数字、符号、功能键、修饰键、空格或删除键。有效首键后，可用第二键高亮，其他字母键灰化。

## 影响

界面更聚焦、占用空间更小。尽管 `Backspace`、`Escape` 和空格仍会被监听以维护状态，它们不在虚拟键盘中显示。
