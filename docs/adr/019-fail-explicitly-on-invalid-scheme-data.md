# ADR 019：方案数据无效时显式失败

## 状态

已接受

## 背景

小鹤映射和合法音节表是候选过滤的唯一依据。若数据格式错误或映射与音节表不一致，静默回退可能向学习者提供错误引导。

## 决策

开发与测试环境中，发现方案数据无效时直接失败并输出清晰错误。正式应用中保留虚拟键盘界面但禁用监听，并在设置中显示配置无效提示；不静默使用不完整映射。

## 影响

数据问题能及早发现且不会误导用户。配置加载层需要明确校验结构、映射和音节一致性，并为错误状态提供可测试的界面反馈。
