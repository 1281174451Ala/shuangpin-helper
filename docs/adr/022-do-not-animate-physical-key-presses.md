# ADR 022：不展示实际按键按下动画

## 状态

已接受

## 背景

候选高亮已明确表达用户需要的下一步双拼提示。额外展示每次物理按键的按下动画会增加视觉变化，并分散对可用第二键的注意力。

## 决策

MVP 不为正常的实际按键提供短暂按下动画。视觉状态仅用于默认、第二键候选和非候选灰化。

## 影响

状态含义保持单一且界面更安静。若未来方案存在无效首键，应按 ADR 007 与 ADR 029 决定是否恢复相应反馈。
