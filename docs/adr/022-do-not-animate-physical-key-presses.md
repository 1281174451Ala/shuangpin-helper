# ADR 022：不展示实际按键按下动画

## 状态

已接受

## 背景

候选高亮已明确表达用户需要的下一步双拼提示。额外展示每次物理按键的按下动画会增加视觉变化，并分散对可用第二键的注意力。

## 决策

MVP 不为正常的实际按键提供短暂按下动画。视觉状态仅用于默认、第二键候选、非候选灰化和无效首键反馈。

## 影响

状态含义保持单一且界面更安静。无效首键的短暂提示是例外，因为其用于明确说明状态未进入等待第二键。
