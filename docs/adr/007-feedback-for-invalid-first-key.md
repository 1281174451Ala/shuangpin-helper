# ADR 007：无效首键的视觉反馈

## 状态

已被 ADR 029 取代

## 背景

在空闲状态下，用户可能输入不能作为当前双拼方案编码首键的字母。界面应提供明确反馈，同时不能让用户误以为仍在等待第二键。

## 决策

当收到无效首键时，仅将实际按下的键以“无效”状态短暂高亮（建议红色，持续约 150 毫秒），不灰化其他按键，随后立即回到空闲状态。

## 影响

该决策保留为未来方案的处理依据。小鹤 MVP 的全部小写字母均为有效首键，当前不实现该反馈；恢复条件见 ADR 029。颜色、动画曲线和精确时长属于视觉设计细节，可在实现时调整。
