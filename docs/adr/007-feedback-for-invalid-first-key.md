# ADR 007：无效首键的视觉反馈

## 状态

已接受

## 背景

在空闲状态下，用户可能输入不能作为当前小鹤双拼编码首键的字母。界面应提供明确反馈，同时不能让用户误以为仍在等待第二键。

## 决策

当收到无效首键时，仅将实际按下的键以“无效”状态短暂高亮（建议红色，持续约 150 毫秒），不灰化其他按键，随后立即回到空闲状态。

## 影响

用户可立即识别无效输入，且不会经历额外等待或错误的候选提示。颜色、动画曲线和精确时长属于视觉设计细节，可在实现时调整。
