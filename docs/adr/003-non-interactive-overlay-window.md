# ADR 003：使用非交互式悬浮窗口

## 状态

已接受

## 背景

用户在其他应用中打字时，辅助工具必须保持可见，但不能抢占焦点或阻挡鼠标交互。

## 决策

虚拟键盘使用无边框、透明、置顶的悬浮窗口展示。该窗口忽略鼠标事件，且不能获取键盘焦点。配置入口放在菜单栏图标中，而非悬浮窗口本身；设置界面负责调整位置、缩放、透明度和是否置顶。

## 影响

悬浮窗口不会打扰正常工作。菜单栏图标成为必要的原生集成，且设置界面必须无需点击悬浮窗口即可打开。
