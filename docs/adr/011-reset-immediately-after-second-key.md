# ADR 011：第二键后立即重置且不展示结果

## 状态

已接受

## 背景

双拼辅助的核心价值是帮助用户判断下一步可按的键，而不是确认已完成的拼音。额外展示识别结果会增加视觉元素并打断连续输入的节奏。

## 决策

收到第二个字母键后，不展示识别出的拼音或编码结果，立即将虚拟键盘恢复为空闲状态。

## 影响

界面持续聚焦于下一次输入的键位查询和候选提示。用户无法通过 MVP 回看刚完成的音节；若后续需要复盘，应作为独立的练习或分析功能设计。
