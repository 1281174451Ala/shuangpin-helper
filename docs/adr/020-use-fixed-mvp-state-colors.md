# ADR 020：MVP 使用固定状态配色

## 状态

已接受

## 背景

候选、灰化和无效等键盘状态必须清晰区分，但主题、颜色选择和配色持久化会扩大设置与视觉测试范围。

## 决策

MVP 使用一套固定的高对比度状态配色，覆盖默认、候选、灰化和无效状态。用户只能调整整体窗口透明度，不能配置主题或单独的状态颜色。

## 影响

界面行为和测试矩阵保持简单。固定配色仍应满足基本可辨识性；后续若加入主题或无障碍配色，需要扩展设置和视觉回归测试。
