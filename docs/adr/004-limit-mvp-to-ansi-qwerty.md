# ADR 004：MVP 限定 ANSI QWERTY 布局

## 状态

已接受

## 背景

双拼映射通常以拉丁字母描述，而 macOS 可使用不同的物理键盘布局。在验证核心学习体验之前，兼容所有布局会增加原生事件归一化和界面布局的复杂度。

## 决策

macOS MVP 仅支持包含 26 个字母键的 ANSI 美式 QWERTY 布局。虚拟键盘和全局按键解释均使用该布局的字母键位。Dvorak、AZERTY 等其他布局不在范围内。

## 影响

初版映射与界面行为保持可预测。未来支持其他布局时，必须明确映射依据物理键位还是实际输出字符，并为每种布局补充兼容性测试。
