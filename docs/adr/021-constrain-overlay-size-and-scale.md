# ADR 021：约束悬浮键盘尺寸与缩放

## 状态

已接受

## 背景

虚拟键盘必须足够紧凑以适合常驻悬浮，同时键帽上的双拼映射文字必须保持可读。

## 决策

默认窗口宽度约为 `420px`，保持三行 QWERTY 字母键的比例。用户可在 `70%` 至 `160%` 范围内缩放窗口。

## 影响

窗口在常见屏幕上占用可控空间，并避免缩放到文字不可读的程度。前端布局应以稳定的网格和最小字号约束实现，并在缩放边界进行视觉测试。
