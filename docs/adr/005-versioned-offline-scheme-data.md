# ADR 005：内置版本化离线方案数据

## 状态

已接受

## 背景

候选按键过滤必须确定、快速、离线可用，且不依赖用户当前输入法或词库。

## 决策

随应用内置版本化 JSON 配置，其中包含小鹤双拼键位映射和现代普通话无声调合法音节表。应用启动时由这些数据推导合法的声母/韵母组合；映射、零声母规则和候选过滤均以单元测试覆盖。

## 影响

引擎行为可复现，并可通过新增配置文件支持其他双拼方案。方案数据修正需随应用版本发布，不依赖远程服务或本地输入法状态。
