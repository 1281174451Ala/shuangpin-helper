# ADR 023：首次启动默认显示并监听

## 状态

已接受

## 背景

双拼辅助工具应在用户启动后立即提供学习价值，不能要求用户先进入设置完成额外操作。

## 决策

首次启动时默认显示悬浮键盘；若已获得辅助功能权限，则立即开始监听。用户可通过菜单栏隐藏窗口或暂停监听，且后续启动恢复其已保存的设置与状态。

## 影响

首次使用路径简洁，后续行为符合用户偏好。未授权时遵循权限降级模式，显示界面但不监听。
