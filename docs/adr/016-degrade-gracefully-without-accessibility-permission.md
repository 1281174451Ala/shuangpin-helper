# ADR 016：缺少辅助功能权限时平稳降级

## 状态

已接受

## 背景

全局键盘监听需要 macOS 辅助功能权限。用户可能拒绝授权、尚未授权，或在系统设置中撤销授权；此时应用仍应保留可理解的界面和操作入口。

## 决策

未获得辅助功能权限时，仍显示虚拟键盘和菜单栏，但不监听按键。设置界面明确显示“监听未启用”，并提供打开系统设置的按钮。不反复弹窗，也不阻塞应用使用；用户授权后可手动恢复监听或重启应用。

## 影响

权限问题不会导致应用不可用或干扰用户。设置界面必须能读取并呈现权限状态，原生层需要支持跳转至 macOS 对应的系统设置页面。
