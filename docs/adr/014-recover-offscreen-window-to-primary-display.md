# ADR 014：不可见窗口恢复到主显示器右下角

## 状态

已接受

## 背景

用户保存的窗口坐标可能因显示器断开、桌面排列变化或分辨率调整而落在当前可见区域之外。悬浮窗口不可点击，若位置丢失将难以自行找回。

## 决策

启动时若保存的位置不在任何当前显示器的可见工作区域内，将窗口放置到主显示器右下角。仍恢复有效的缩放、透明度和置顶设置。

## 影响

用户始终能找到悬浮键盘。窗口位置恢复逻辑需要读取当前显示器工作区域，并对多显示器切换进行原生测试。
