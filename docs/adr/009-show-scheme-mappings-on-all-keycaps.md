# ADR 009：所有键帽常驻显示方案映射

## 状态

已接受

## 背景

用户需要随时查询小鹤双拼的声母和韵母映射，不能只在按键后才获得提示。

## 决策

虚拟键盘的每个键帽始终显示大写英文字母，以及该键对应的小鹤声母和/或韵母映射。键帽可用分行或小字并列的方式组织这些信息；没有映射的键只显示字母。

## 影响

空闲界面本身即为键位速查表，并在候选过滤时继续提供含义上下文。具体排版必须在小尺寸窗口下保持可读性。
