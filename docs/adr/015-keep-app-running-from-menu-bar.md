# ADR 015：以菜单栏作为应用生命周期入口

## 状态

已接受

## 背景

双拼辅助工具应常驻后台。用户可能误关闭设置窗口或悬浮窗口，但仍希望随时从菜单栏恢复辅助功能。

## 决策

关闭设置窗口或悬浮窗口时，仅隐藏对应窗口，应用继续在菜单栏运行。只有用户通过菜单栏的“退出”操作时，应用进程才真正结束；菜单栏还应提供重新显示悬浮窗口的入口。

## 影响

应用行为符合常驻 macOS 工具的预期。原生窗口关闭事件必须转为隐藏，而菜单栏菜单需要明确区分显示、暂停和退出操作。
