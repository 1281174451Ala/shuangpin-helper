# ADR 024：自动测试核心逻辑，手动验收原生集成

## 状态

已接受

## 背景

双拼规则和状态转移可在不依赖 macOS 的环境中稳定测试；全局监听权限和多显示器窗口行为则依赖真实系统环境。

## 决策

自动化测试必须覆盖小鹤映射加载、普通音节与零声母合法性、候选过滤、状态重置，以及 Rust 的按键归一化和权限状态转换。真实 macOS 辅助功能权限流程与多显示器行为作为手动验收项。

## 影响

核心规则获得快速、可重复的回归保护，同时将平台依赖行为放在实际设备上验证。测试文档需要明确记录手动验收步骤和预期结果。
