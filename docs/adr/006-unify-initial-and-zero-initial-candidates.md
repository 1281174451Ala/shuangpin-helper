# ADR 006：统一声母与零声母的候选计算

## 状态

已接受

## 背景

小鹤双拼同时编码有声母和零声母音节。若使用独立的界面逻辑，可能会让 `an`、`ai`、`ou` 等有效音节得到不一致的候选反馈。

## 决策

所有可用第一键均通过同一个数据驱动的候选查询解释。该查询覆盖普通声母、单字母声母和小鹤零声母规则，只高亮能完成至少一个合法音节的第二键。

## 影响

状态机保持简单，规则集中在方案数据中。测试必须通过同一候选过滤接口覆盖普通声母和零声母音节。
