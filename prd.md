# 双拼辅助虚拟键盘工具 - 产品需求文档（PRD）

## 1. 项目概述

开发一个独立于输入法的桌面辅助工具，用于帮助用户学习和熟练使用双拼输入法。

软件以悬浮窗口形式运行在屏幕角落，通过监听用户全局键盘输入，实时展示当前双拼输入状态，并根据输入状态动态提示合法按键。

目标用户：

* 正在学习双拼的新用户
* 希望提高双拼熟练度的用户
* 使用小鹤双拼的开发者

---

# 2. 核心目标

解决双拼学习中的主要痛点：

1. 用户不知道每个键对应哪些声母/韵母
2. 用户不知道当前输入后哪些键是合法的下一步
3. 用户容易混淆相似韵母映射
4. 用户需要在真实输入环境中形成肌肉记忆

通过实时反馈降低学习成本。

---

# 3. 产品形态

## 3.1 桌面悬浮应用

形式：

* 独立桌面应用
* 常驻后台运行
* 屏幕角落显示虚拟键盘
* 不影响用户正常输入

窗口要求：

* 无边框窗口
* 支持置顶
* 支持透明背景
* 支持调整大小
* 不抢占当前输入焦点

默认位置：

* 屏幕右下角

---

# 4. 支持平台

第一阶段：

* macOS

后续：

* Windows
* Linux

---

# 5. 双拼方案支持

第一版本支持：

## 小鹤双拼

包含：

* 声母映射
* 韵母映射
* 零声母规则

例如：

```
zh -> v
ch -> i
sh -> u

ang -> h
eng -> g
ing -> y
ong -> s
```

设计要求：

未来可扩展其他方案：

* 微软双拼
* 自然码
* 搜狗双拼

方案配置化：

示例：

```json
{
  "name": "xiaoheshuangpin",
  "initials": {},
  "finals": {}
}
```

---

# 6. 核心功能需求

## 6.1 虚拟键盘展示

展示完整 QWERTY 键盘。

每个键显示：

* 英文字母
* 对应双拼编码信息

例如：

```
┌────┐
│ H  │
│ang │
└────┘
```

显示内容：

声母键：

```
h
```

韵母键：

```
ang
```

---

## 6.2 全局键盘监听

软件需要监听用户键盘事件。

要求：

* 不影响系统输入
* 不修改输入内容
* 不作为输入法存在

监听：

* keyDown
* keyUp

关注：

* 字母键
* 删除键
* ESC
* 空格

---

## 6.3 输入状态识别

维护双拼输入状态。

状态：

### 空闲状态

所有按键正常显示。

---

### 输入第一个键

例如：

用户输入：

```
d
```

系统判断：

可能组合：

```
da
de
di
du
dan
dang
...
```

显示：

已输入：

```
D
```

高亮：

可作为第二键的字符。

其他键：

置灰。

---

### 输入第二个键

例如：

```
dh
```

识别：

```
dang
```

完成一个双拼编码。

状态重置。

---

# 7. 智能按键过滤

核心功能。

根据当前输入状态动态计算合法按键。

例如：

初始：

```
所有键可用
```

输入：

```
d
```

计算：

所有合法拼音：

```
da
de
di
du
dan
dang
den
...
```

得到下一步：

```
a e i u n h ...
```

效果：

可用：

```
A E I U H
```

不可用：

```
Q W R T ...
```

---

# 8. 拼音合法性引擎

需要维护：

## 声母 + 韵母合法组合表

例如：

```
d:
 da
 de
 di
 dou
 dan
 dang
 deng
```

支持：

* 普通拼音
* 零声母

数据结构建议：

Trie 或状态机。

目标：

快速判断：

```
当前输入 + 下一个键
是否可能组成合法拼音
```

---

# 9. 交互效果

## 键盘状态

三种状态：

### 默认

普通显示。

---

### 可输入

高亮显示。

---

### 不可输入

灰色显示。

---

## 动画

支持：

* 按键缩放
* 高亮动画
* 状态切换动画

动画不影响性能。

---

# 10. 设置功能

提供设置页面。

支持：

## 显示设置

* 窗口位置
* 窗口大小
* 透明度
* 是否置顶

## 输入方案

选择：

* 小鹤双拼

未来：

* 微软双拼
* 自然码

## 开机启动

支持：

* 开机自动启动
* 后台运行

---

# 11. 技术要求

推荐技术栈：

## 桌面框架

Tauri 2

原因：

* 轻量
* 支持系统 API
* 适合常驻工具

## 前端

React + TypeScript

负责：

* UI
* 键盘渲染
* 动画
* 设置页面

## 后端

Rust

负责：

* 全局键盘监听
* 窗口控制
* 系统权限

---

# 12. 推荐项目结构

```
double-pinyin-helper

├── src
│   ├── components
│   │   ├── VirtualKeyboard
│   │   ├── Key
│   │   └── Settings
│   │
│   ├── engine
│   │   ├── shuangpin.ts
│   │   ├── pinyinTrie.ts
│   │   └── stateMachine.ts
│   │
│   └── store
│
├── src-tauri
│   ├── keyboard_listener.rs
│   └── window.rs
│
└── config
    └── shuangpin.json
```

---

# 13. MVP 第一阶段范围

必须完成：

* [ ] macOS运行
* [ ] 悬浮窗口
* [ ] 虚拟键盘显示
* [ ] 小鹤双拼键位展示
* [ ] 全局键盘监听
* [ ] 当前按键高亮
* [ ] 非候选键灰化
* [ ] 基础拼音合法性判断

---

# 14. 后续增强方向

## 练习模式

输入：

```
中华人民共和国
```

显示目标：

```
vs rm rg
```

统计：

* 正确率
* 速度
* 常错键

## 学习分析

记录：

* 高频错误组合
* 易混淆韵母

例如：

```
你的错误：

ang(h)
eng(g)

建议练习：
上海银行
```

---

# 15. 验收标准

完成后：

1. 用户打开软件即可看到虚拟键盘
2. 用户正常使用任意输入法时，工具能够显示按键状态
3. 输入第一个双拼键后，正确过滤下一步候选
4. 键盘状态实时变化，无明显延迟
5. 不影响用户正常打字
6. 软件长期运行稳定，占用资源低

---

# 项目定位

这是一个：

“帮助用户学习双拼的智能虚拟键盘助手”

而不是：

“新的输入法”。

软件不负责中文输入，只负责：

理解双拼状态 → 提供实时视觉反馈 → 加速形成肌肉记忆。
